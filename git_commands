renaming file: 
git mv duckdb_install.txt duckdb_installation

###############################
to exit the terminal:
type :q
###################
RENAMING OF FILES
git config commands:
    git config --global alias.c commit
######################

git log commands: